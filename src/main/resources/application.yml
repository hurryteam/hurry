spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: test
    password: test1234
    url: jdbc:mysql://127.0.0.1:3306/hurry?characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
  jpa:
    show-sql: true
  jackson:
    serialization:
      write-dates-as-timestamps: true

server:
  servlet:
    context-path: /hurry